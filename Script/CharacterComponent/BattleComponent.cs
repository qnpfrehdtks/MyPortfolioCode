using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleComponent : MonoBehaviour
{

    //public void Init(CharacterBase character)
    //{
        
    //    m_Character = character;
    //}

    //public void Damaged(DamageInfo damageInfo)
    //{
    //    int dmg = 0;
    //    dmg = m_Character.CaculateDamageResult(damageInfo);

    //    if (damageInfo.isCriAttack)
    //    {
    //        UIManager.Instance.ShowHUDText(transform.position + Vector3.up * 1.0f + Vector3.up * 1.0f, $"{dmg}", Color.white, 0.4f, 12);
    //    }
    //    else
    //    {
    //        UIManager.Instance.ShowHUDText(transform.position + Vector3.up * 1.0f + Vector3.up * 1.0f, $"{dmg}", Color.red, 0.4f, 18);
    //    }

    //    m_Character.m_characterStat.DoDamageHP(dmg);
    //}

}
